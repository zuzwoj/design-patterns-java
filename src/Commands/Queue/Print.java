package Commands.Queue;
import java.io.Console;
import java.util.Queue;

public class Print /*extends Commands.Generic*/{
    /*private Queue<genericCommand> commandQueue;
        public queuePrintCommand(Queue<genericCommand> commandQueue) { this.commandQueue = commandQueue; }
        public override string CommandName { get { return "queue print"; } set { } }
        public override commandArguments Arguments { get; set; }
        public override bool Dialogue() { return true; }
        public override void Execute() 
        {
            if (Arguments.Arguments.Length > 0) Console.WriteLine("Wrong arguments, try queue print");
            else foreach (var item in commandQueue) Console.WriteLine(item.ToString()); 
        }*/
}