package Commands.Queue;
import java.io.Console;
import java.util.Queue;

public class Commit /*extends Commands.Generic*/{
    /*private Queue<genericCommand> commandQueue;
        public queueCommitCommand(Queue<genericCommand> commandQueue) { this.commandQueue = commandQueue; }
        public override string CommandName { get { return "queue commit"; } set { } }
        public override commandArguments Arguments { get; set; }
        public override bool Dialogue() { return true; }
        public override void Execute()
        {
            if (Arguments.Arguments.Length > 0) Console.WriteLine("Wrong arguments, try queue commit");
            while (commandQueue.Count > 0)
            {
                var command = commandQueue.Dequeue();
                command.Execute();
            }
        }*/
}